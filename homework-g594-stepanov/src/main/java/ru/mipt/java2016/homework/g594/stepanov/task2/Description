Описание работы.

Заведём фабрику (SerializationFactory), которая по паре переданных типов (в виде строк) создаст
сериализатор и кэшированное хранилище для этой пары типов.

Сериализатор будет получаться из наследников ObjectSerializator. В абстрактном классе реализован метод
validate, проверяющий валидность состояния хранилища путем сравнения хэша с записанным.